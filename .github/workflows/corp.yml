name: corp

on:
  pull_request:
    paths:
      - "corp/**"
      - ".github/workflows/corp.yml"
  push:
    branches: [main]
    paths:
      - "corp/**"
      - ".github/workflows/corp.yml"

jobs:
  test:
    name: test and build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: corp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: corp/package-lock.json
      - run: npm ci
      - run: npm test
      - run: npm run build
